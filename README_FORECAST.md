# Forecast guide (legacy)

This file is kept for backward compatibility only.

The integration now has built-in support for EPEX Predictor and no longer requires the separate custom forecast sensor flow described in earlier versions of this document.

Please use the main README instead:

- Predictor usage and behavior are documented in [README.md](README.md).
- Dynamic dashboard period examples are documented in the section `Dynamic dashboard period (recommended)` in [README.md](README.md).

## Why this file still exists

Some external links, bookmarks, and older discussions still point to `README_FORECAST.md`.
Keeping this file avoids broken links while steering users to the current setup in `README.md`.
